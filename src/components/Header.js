import React from 'react';
// Styles
import '../styles/header.scss';

const Header = () => (
  <header className="header">
    <h1 className="header-title">My to-do sticky notes</h1>
  </header>
);

export default Header;
